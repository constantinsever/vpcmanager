version: 1

frontend:
phases:
preBuild:
commands:
- echo "Generating config.js"

```
    - |
      cat > frontend/config.js <<EOF
      window.APP_CONFIG = {
        env: "$APP_ENV",
        apiBaseUrl: "$API_BASE_URL"
      };
      EOF

build:
  commands:
    - echo "Frontend build completed -- pentru JS"
```

artifacts:
baseDirectory: frontend
files:
- '**/*'

cache:
paths: []
