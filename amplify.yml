version: 1


frontend:
  phases:
    preBuild:
      commands:
        - |
          cat > frontend/config.js <<EOF
          window.APP_CONFIG = {
            env: "$APP_ENV",
            apiBaseUrl: "$API_BASE_URL"
          };
          EOF

  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'